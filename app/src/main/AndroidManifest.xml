<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fernandez.pablo.la24gnc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".View.MainActivity">

        </activity>
        <activity android:name=".View.AbrirTurnoActivity" />
        <activity android:name=".View.VentaActivity" />
        <activity android:name=".View.DescuentoActivity" />
        <activity android:name=".View.CerrarTurnoActivity" />
        <activity
            android:name=".View.DetalleTurnoActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".View.MainV2Activity"
            android:label="@string/title_activity_main_v2"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>